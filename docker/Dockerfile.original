# BEFORE — Insecure Dockerfile (Acme's original approach)
# Problems: runs as root, full Node image, single stage, copies everything

FROM node:24
WORKDIR /app
COPY . .
RUN npm install
EXPOSE 3000
USER root
CMD ["npm", "start"]
